# Focus Grid Environment Variables

# Database Configuration (Neon PostgreSQL)
DATABASE_URL=postgresql://username:password@ep-example.us-east-1.aws.neon.tech/neondb?sslmode=require

# Client Configuration (for client instances)
CLIENT_ID=demo
CLIENT_NAME="Demo Client"

# Feature Flags (for client instances)
ENABLE_LONG_TERM_GOALS=true
ENABLE_ACTION_PLAN=true
ENABLE_BLOCKERS_ISSUES=true
ENABLE_AGENDA=true
ENABLE_FOCUS_MODE=true

# Branding (for client instances)
PRIMARY_COLOR="#346.8 77.2% 49.8%"
CLIENT_LOGO_URL=""

# Application Mode
APP_MODE=client  # Options: "admin" (main version) or "client" (client instance)

# Admin Configuration (for main/admin version only)
ADMIN_SECRET=your-admin-secret-key

# Netlify Integration (for admin version only)
NETLIFY_ACCESS_TOKEN=your-netlify-personal-access-token
